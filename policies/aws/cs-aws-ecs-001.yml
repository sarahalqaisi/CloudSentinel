id: CS-AWS-ECS-001
title: Container is configured as privileged
description: Detects cloud infrastructure configurations that violate the CloudSentinel
  Baseline security baseline.
severity: critical
category: Containers
provider: aws
framework: CloudSentinel Baseline
control_id: ECS-PRIV-01
scope: resource
resource_types:
- aws_ecs_task_definition
check: privileged_container
remediation: Remove privileged mode and grant only the Linux capabilities the workload
  requires.
enabled: true
version: 1
