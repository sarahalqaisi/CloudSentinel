[project]
name = "cloudsentinel"
version = "1.0.0"
description = "Cloud security posture, Terraform policy-as-code, compliance, and DevSecOps assessment platform"
requires-python = ">=3.11"
license = {text = "MIT"}

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
